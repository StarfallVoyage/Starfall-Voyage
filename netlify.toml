[build]
  publish = "."

# After Render gives you your live multiplayer server URL, uncomment this block
# and replace YOUR-RENDER-SERVICE with your actual service name.
#
# [[redirects]]
#   from = "/api/*"
#   to = "https://YOUR-RENDER-SERVICE.onrender.com/api/:splat"
#   status = 200
#   force = true
